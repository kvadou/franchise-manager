// Short wiki URL — reuses the full knowledge base article page.
// The article page reads params.slug || params.id, so this works.
export { default } from "@/app/(prospect)/portal/learning/knowledge-base/[id]/page";
